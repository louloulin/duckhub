[package]
name = "duckhub-database"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
# Core dependencies
duckhub-common = { path = "../common" }

# Async
tokio.workspace = true
async-trait.workspace = true

# Database (temporarily disabled due to compilation issues)
# # duckdb.workspace = true
redis.workspace = true

# Serialization
serde.workspace = true
serde_json.workspace = true

# Error handling
anyhow.workspace = true
thiserror.workspace = true

# Logging
tracing.workspace = true

# Time and UUID
uuid.workspace = true
chrono.workspace = true

# Utilities
once_cell.workspace = true
dashmap.workspace = true
parking_lot.workspace = true

# Object storage (temporarily disabled due to compilation issues)
# # aws-sdk-s3.workspace = true
# # aws-config.workspace = true
# azure_storage.workspace = true
# azure_storage_blobs.workspace = true

# Data formats (temporarily disabled due to arrow-arith conflicts)
# parquet.workspace = true
# arrow.workspace = true

# Metrics
prometheus.workspace = true

# Encoding
base64 = "0.21"

[dev-dependencies]
tokio-test = "0.4"
tempfile.workspace = true
mockall.workspace = true
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "ducklake_benchmarks"
harness = false
